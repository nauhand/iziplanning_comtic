document.querySelectorAll('.show_pick').forEach(item => {
    item.addEventListener('click', function(e){
      e.preventDefault;
      document.querySelector('#current_dossier').setAttribute('src', this.getAttribute('data-src'));
    })
  })